server:
  port: 8080

spring:
  application:
    name: gateway-proxy

zuul:
  prefix: /api
  ignoredServices: '*'
  routes:
    customer-service:
      path: /customer/**
      stripPrefix: false
    account-service:
      path: /account/**
      stripPrefix: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka