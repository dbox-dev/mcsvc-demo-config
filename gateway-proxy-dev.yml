server:
  port: 8080

spring:
  application:
    name: gateway-proxy

zuul:
  prefix: /api
  ignoredServices: '*'
  routes:
    customer-service:
      path: /customer/**
      stripPrefix: false
    account-service:
      path: /account/**
      stripPrefix: false
      
customer-service:
  ribbon:
    listOfServices: http://localhost:8081/
account-service:
  ribbon:
    listOfServices: http://localhost:8081/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka
      fetch-registry: true